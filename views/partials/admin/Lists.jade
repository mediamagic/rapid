.page.secondary
	.page-header 
		.page-header-content
			h1 Items
				small &nbsp;
			a.back-button.big.page-back(href="#/main")
.page
	.page-region
		.page-region-content
			.grid
				.row
					.span12
						select(ng-model="filterCategory")
							option(value="0") Select Category
							option(ng-repeat="cat in categories", value="{{ cat }}") {{ cat }}						
						ul#listWrapper(ui-sortable="sortableOptions", ng-model="category[filterCategory]")
							//- li.itemInList(ng-repeat="item in category[filterCategory] | orderBy:orderCategories") {{ item.sidebar.title }}
							li.itemInList(ng-repeat="item in category[filterCategory]") {{ item.sidebar.title }}
						br	
						button(ng-click="previewNewList()") Preview
						button(ng-disabled="showListsSaveButton") Save	
						
						//- pre(ng-bind="category[filterCategory] | json | orderBy:cat.categories[filterCategory]")

						//- span(ng-repeat="cat in category[filterCategory] | orderBy:orderCategories")
						span(ng-repeat="cat in category[filterCategory]")
							.s {{ cat.categories[filterCategory] }} {{ cat.sidebar.title }}
						
						//- h3.padding5.bg-color-red.fg-color-white
						//- 	i.icon-user-2
						//- 	span  Cookies
						//- table.bg-color-red.fg-color-white
						//- 	thead
						//- 		tr
						//- 			th image
						//- 			th name
						//- 			th number of votes
						//- 			th link
						//- 	tbody
						//- 		tr(ng-repeat="user in users")
						//- 			td.padding5(style="width: 50px")
						//- 					img(ng-src="/images/cookie_{{user.name}}.png", width="50")
						//- 			td {{user.name}}	
						//- 			td(style="width: 100px;") {{user.votes}}
						//- 			td(style="text-align: center; width: 30px;")
						//- 				a(href="#/nominee/{{user._id}}", style="color: black; text-align-center;")
						//- 					i.icon-eye.fg-color-black
